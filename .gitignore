*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx

# Never commit the plaintext debug keystore. Only the AES-encrypted zip
# (keystore/debug-keystore.zip) is tracked; CI decrypts it with a Secret.
keystore/debug.keystore
